# ── Database ──────────────────────────────────────────────────────────────────
# SQLite (default for local development — no extra service required)
DATABASE_URL=sqlite:////data/app.db

# PostgreSQL (uncomment for production or to match docker-compose.prod.yml)
# DATABASE_URL=postgresql+psycopg2://postgres:password@db:5432/urlshortener

# ── PostgreSQL credentials (used by docker-compose.prod.yml) ──────────────────
POSTGRES_DB=urlshortener
POSTGRES_PASSWORD=change-me-in-production

# ── Application ───────────────────────────────────────────────────────────────
# Number of uvicorn workers (set to 1 for local dev, 4+ for production)
WORKERS=1

# ── Security ──────────────────────────────────────────────────────────────────
SECRET_KEY=your-secret-key-here
